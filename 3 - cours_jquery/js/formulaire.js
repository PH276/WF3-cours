$(function(){
    $("#date-field").datepicker(); // initialise le champ pour qu'il utilise jQuery UI

    $("#range-field").slider(); // initialise le champ pour qu'il utilise jQuery UI
    $('#color-field').iris(); // initialise le champ pour qu'il utilise jQuery UI
})
